{
  "name": "sparkpos-backend",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "SparkPOS backend — Express + Mongoose API",
  "scripts": {
    "dev": "node --watch --env-file=.env src/index.js",
    "start": "node --env-file=.env src/index.js",
    "test": "node --test \"tests/**/*.test.js\"",
    "rs:init": "node src/scripts/initReplicaSet.js"
  },
  "dependencies": {
    "bcrypt": "^6.0.0",
    "express": "^5.2.1",
    "mongoose": "^9.7.1",
    "multer": "^2.2.0",
    "papaparse": "^5.5.4",
    "sharp": "^0.35.2"
  }
}
